This folder contains 2 jnlp files. 

Only the install.jnlp is generated. It refers to itself so as to avoid to provide the repository on a website

The other file (webinstall.jnlp), contains a codebase reference